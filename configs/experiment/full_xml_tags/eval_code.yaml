# @package _global_
# Experiment: Evaluate on code_selection with full_xml_tags
#
# Usage:
#   python -m src.eval experiment=full_xml_tags/eval_code \
#       training_group=leave_out_code_full_xml_tags_seed_42 \
#       training_run_name=monitor_informed_pen \
#       artifact_step=100

defaults:
  - override /data: leave_out_code_full_xml
  - override /eval: code

config_name: code_eval_formatted
